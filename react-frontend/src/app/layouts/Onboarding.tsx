import { Outlet } from "react-router";

const Onboarding = () => {
    return (
        <main>
            Onboarding Flow
            <Outlet />
        </main>
    );
};

export default Onboarding;
